# OS / editor
*~
*.swp
.DS_Store

# Schemas build artifact is committed for easy install; rebuild with install.sh
# Keep gschemas.compiled for clone-and-run convenience.

# Built extension packs (create with gnome-extensions pack)
*.shell-extension.zip
